component:
  write:
    connection: bigquery_data_plane
    input:
      name: feedback
      flow: elt
    bigquery:
      table:
        name: elt_feedback
        dataset: ${parameters.bigquery_dataset}
