profile:
  parameters:
    gcp_project_id: ascend-io-dev-sean
    bigquery_dataset: QUICKSTART_DEV
    bigquery_location: US

  defaults:
    - kind: Flow
      name:
        regex: .*
      spec:
        data_plane:
          connection_name: bigquery_data_plane
