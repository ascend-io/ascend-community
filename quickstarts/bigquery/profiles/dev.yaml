profile:
  parameters:
    gcp_project_id: ascend-io-cody
    bigquery_dataset: QUICKSTART_DEV_OLD
    bigquery_location: US

  defaults:
    - kind: Flow
      name:
        regex: .*
      spec:
        data_plane:
          connection_name: bigquery_data_plane
