---
otto:
  rule:
    alwaysApply: true
    description: Fabric uses the T-SQL dialect.
---

# T-SQL dialect

This Project uses Microsoft Fabric as its data plane. Fabric uses the T-SQL dialect (e.g. use `SELECT TOP N ...` instead of `... LIMIT N`).
