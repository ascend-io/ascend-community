flow:
  version: 0.1.0
  parameters:
    bigquery:
      dataset: ${parameters.gcp.bigquery.dataset}_TRANSFORM
    defaults:
      - kind: Component
        name:
          regex: ".*"
        spec:
          retry_strategy:
            stop_after_attempt: 5
