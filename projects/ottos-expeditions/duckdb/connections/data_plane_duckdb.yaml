connection:
  duckdb:
    max_concurrent_queries: ${parameters.duckdb.max_concurrent_queries:-32}
    ducklake:
      $<: $parameters.duckdb.ducklake
      catalog: ${parameters.duckdb.ducklake.catalog:-}
