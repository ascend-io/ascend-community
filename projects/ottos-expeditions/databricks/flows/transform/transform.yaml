flow:
  version: 0.1.0
  parameters:
    databricks:
      schema: TRANSFORM
      http_path: ${parameters.databricks.warehouse_http_path}
  defaults:
    - kind: Component
      name:
        regex: ".*"
      spec:
        retry_strategy:
          stop_after_attempt: 5
