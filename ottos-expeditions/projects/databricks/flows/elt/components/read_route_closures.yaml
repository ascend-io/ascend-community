component:
  read:
    connection: local_files
    local_file:
      parser: auto
      path: router_closures.csv
  data_plane:
    databricks:
      table_properties:
        delta.columnMapping.mode: name
