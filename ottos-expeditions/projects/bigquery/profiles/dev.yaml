profile:
  parameters:
    gcp:
      project_id: ascend-io-dev-sean
    bigquery:
      dataset: OTTOS_EXPEDITIONS_DEV
      location: US

  defaults:
    - kind: Flow
      name:
        regex: .*
      spec:
        data_plane:
          connection_name: bigquery_data_plane
