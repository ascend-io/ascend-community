automation:
  enabled: true
  name: nightly
  triggers:
    sensors:
      - type: timer
        name: nightly-timer
        config:
          schedule:
            cron: 0 6 * * *
  actions:
    - type: run_flow
      name: run-etl
      config:
        flow: elt
