WITH twitter AS (
    SELECT
        *
    FROM
        {{ ref("read_twitter", flow="extract-load-bigquery", reshape="map") }}
)
SELECT
    *
FROM
    twitter
ORDER BY
    timestamp DESC

{{ with_test("not_null", column="timestamp") }}

