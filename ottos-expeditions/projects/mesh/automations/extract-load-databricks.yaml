automation:
  enabled: true
  name: extract-load-databricks
  triggers:
    sensors:
      - type: timer
        name: cron-timer
        config:
          schedule:
            cron: 0 * * * *
  actions:
    - type: run_flow
      name: run-extract-load-databricks
      config:
        flow: extract-load-databricks
