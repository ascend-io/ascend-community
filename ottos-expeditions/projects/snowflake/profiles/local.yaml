profile:
  parameters:
    snowflake:
      account: ascendpartner
      user: ASCEND_IO_SAMPLE_DATA
      database: ascend_pipeline_test
      schema: rui
      warehouse: TEST
      role: ""

  defaults:
    - kind: Flow
      name:
        regex: .*
      spec:
        data_plane:
          connection_name: snowflake_data_plane
