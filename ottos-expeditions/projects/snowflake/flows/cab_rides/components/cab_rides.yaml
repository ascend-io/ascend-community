component:
  read:
    connection: local_files
    event_time: pickup_datetime
    local_file:
      path: cab_rides/
      parser: parquet
      include:
        - suffix: parquet
      time_based_file_selection:
        pattern: 'partition_column=(?P<year>\d{4})-(?P<month>\d{2})-(?P<day>\d{2})'
