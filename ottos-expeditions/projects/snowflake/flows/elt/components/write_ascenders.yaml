component:
  write:
    connection: snowflake_data_plane
    input:
      name: ascenders
      flow: elt
    snowflake:
      table:
        name: elt_ascenders
        schema: ${parameters.snowflake.schema}

