component:
  read:
    connection: gcs_lake
    gcs:
      path: /lakev0/website_events/
      include:
        - suffix: parquet
        - glob: "**/year=*/month=*/day=*/*.parquet"
      parser: auto