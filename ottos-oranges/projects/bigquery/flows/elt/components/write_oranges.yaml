component:
  write:
    connection: bigquery_data_plane
    input:
      name: oranges
      flow: elt
    bigquery:
      table:
        name: elt_oranges
        dataset: ${parameters.bigquery_dataset}
