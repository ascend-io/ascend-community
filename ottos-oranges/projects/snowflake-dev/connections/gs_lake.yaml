#connection:
#  gcs:
#    root: gs://${parameters.lake.gcs_bucket}
#    project: ${parameters.lake.gcp_project}
connection:
  gcs:
    root: ${parameters.lake.gcs_bucket}
    project: ${parameters.lake.gcp_project}
