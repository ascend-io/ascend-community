component:
  read:
    connection: az_lake
    abfs:
      path: lakev1/website_events/year=2025/month=1/day=1/data_0.parquet

      #include:
      #  - glob: "*/day=*/*.parquet"
      # parser: auto
