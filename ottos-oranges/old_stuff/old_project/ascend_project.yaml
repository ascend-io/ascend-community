project:
  name: "Otto's Oranges"
  connections: ["connections/"]
  flows: ["flows/"]
  profiles: ["profiles/"]
  sources: ["src/"]
  vaults: ["vaults/"]

  # TODO: remove in favor of pyproject.toml
  pip_packages:
    - rich
    - typer
    - shiny
    - shinyswatch
    - shinywidgets
    - 'ibis-framework[sqlite,duckdb,datafusion,polars,deltalake]==9.5.0'
    - Faker
    - plotly
    - pydantic-ai
